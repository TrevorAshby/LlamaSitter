# llamasitter desktop config path

Print the app-managed config path

## Usage

```text
Usage:
  llamasitter desktop config path [flags]

Global Flags:
  -c, --config string   path to config file (default "llamasitter.yaml")
```
