# llamasitter desktop db path

Print the app-managed SQLite database path

## Usage

```text
Usage:
  llamasitter desktop db path [flags]

Global Flags:
  -c, --config string   path to config file (default "llamasitter.yaml")
```
