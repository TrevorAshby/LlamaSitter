# llamasitter config privacy add-redact-json-field

Add one field name to redact_json_fields

## Examples

```text
llamasitter config privacy add-redact-json-field messages
```

## Usage

```text
Usage:
  llamasitter config privacy add-redact-json-field NAME [flags]

Examples:
  llamasitter config privacy add-redact-json-field messages

Flags:
      --dry-run   print the updated config without writing it

Global Flags:
  -c, --config string   path to config file (default "llamasitter.yaml")
```
